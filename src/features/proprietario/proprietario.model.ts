//import { IUsuarioRow } from "../usuario/usuario.model";

/**
 * Interface que define o formato das linhas retornadas pelo banco de dados 
 * especificamente para a entidade Proprietario.
 * Ela estende IUsuarioRow pois um Proprietário é, no banco de dados, 
 * uma extensão da tabela de usuários e pessoas.
 */
//export interface IProprietarioRow extends IUsuarioRow {
  // Atualmente a tabela 'proprietarios' serve como vínculo (FK), 
  // mas campos específicos de proprietário entrariam aqui no futuro.
//}